{% extends '::base.html.twig' %}

{% block title %}Offer - new{% endblock %}

{% block body -%}
    <h1>Offer creation</h1>

    <form action="{{ path('offer_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions btn-group">
	    <li>
	        <a class="btn" href="{{ path('offer') }}">Back to the list</a>
    	</li>
	</ul>
{% endblock %}
